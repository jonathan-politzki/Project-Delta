{
  "name": "writer-analysis-tool",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd backend && uvicorn main:app --host=0.0.0.0 --port=$PORT",
    "build": "cd frontend && npm install && npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "openai": "^4.54.0"
  }
}
