{
  "name": "writer-analysis-tool",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "react-scripts build",
    "postbuild": "ls -R build",      
    "start": "cd backend && uvicorn main:app --host=0.0.0.0 --port=$PORT",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "openai": "^4.54.0"
  }
}
